<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% block title %}Tiny programming blog{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/base.css') }}"/>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        {# {{ encore_entry_link_tags('app') }} #}
    {% endblock %}

    {% block javascripts %}
        {# {{ encore_entry_script_tags('app') }} #}
    {% endblock %}
</head>
<body>
{% block body %}
    {% block navbar %}
        <div class="navbar">
            <div class="row">
                <a href="{{ path('home') }}">
                    <h1>
                        Well, yes... But how to code it?
                    </h1>
                </a>
            </div>
            <div class="topics">
                <a href="{{ path('category', {'categoryName': 'Database'}) }}">Databases</a>
                <a href="{{ path('category', {'categoryName': 'Oracle'}) }}">Oracle</a>
                <a href="{{ path('category', {'categoryName': 'UML'}) }}">UML</a>
                <a href="{{ path('category', {'categoryName': 'Symfony'}) }}">Symfony</a>
                <a href="{{ path('category', {'categoryName': 'Python'}) }}">Python</a>
                <a href="{{ path('category', {'categoryName': 'Flutter'}) }}">Flutter</a>
            </div>
        </div>
    {% endblock %}
    {% block main %}
        <div class="main" style="background-image: url('{{ asset('images/background.jpg') }}')">
            <div class="column" style="text-align: left;">
                <h3>Tiny programming blog</h3>
                <a href="{{ path('home') }}">
                    <button type="button">ALL ARTICLES ></button>
                </a>
            </div>
        </div>
    {% endblock %}
    {% block page_content %}
    {% endblock %}
    {% block footer %}
        <div class="footer">
            Designed by Marta Mazurkiewicz, 2021
            <br>
            Write me at email
        </div>
    {% endblock %}
{% endblock %}
</body>
</html>
