{% extends ('base.html.twig') %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}
{% block page_content %}
    <div class="section">
    <p class="section">All Articles</p>
    {% for article in articles %}
        <div class="row article">

            <div class="column-7 relative">
                <img src="images\{{ article.getId }}.jpg" alt="{{ article.getId }}"/>
            </div>
            <div class="column-5 articleSummary">
                <p>{{ article.getCategory.getNameEn }} / {{ article.getReleaseDate|date("Y/m/d") }}</p>
                <h2 class="titleHeadline"><a href="#">{{ article.getTitleEn }}</a></h2>
                {% for content in article.getContentEns() %}
                    <p>{{ content.getText }}</p>
                {% endfor %}
                <button type="button">READ ARTICLE ></button>
            </div>
        </div>
        {# <div class="formGroup"> #}
        {# {{ form_label(form.rangeStart) }} #}
        {# <div class="formInput"> #}
        {# {{ form_widget(form.rangeStart) }} #}
        {# </div> #}
        {# </div> #}
        {# <div class="formGroup"> #}
        {# {{ form_label(form.sizeOfDataset) }} #}
        {# <div class="formInput"> #}
        {# {{ form_widget(form.sizeOfDataset) }} #}
        {# </div> #}
        {# </div> #}
        {# <div class="formGroup"> #}
        {# {{ form_label(form.mean) }} #}
        {# <div class="formInput"> #}
        {# {{ form_widget(form.mean) }} #}
        {# </div> #}
        {# </div> #}
        {# <div class="formGroup" style="margin-bottom: 15px;"> #}
        {# {{ form_label(form.standardDeviation) }} #}
        {# <div class="formInput"> #}
        {# {{ form_widget(form.standardDeviation) }} #}
        {# </div> #}
        {# </div> #}
        {# <div class="text-center"> #}
        {# {{ form_widget(form.submit) }} #}
        {# </div> #}
        {# {{ form_end(form) }} #}
        {# </div> #}
        {# <div class="column-5 articleSummary"> #}
        {# <p>68% of observations are in range between {{ sigmas[0][0] }} and {{ sigmas[0][1] }}</p> #}
        {# <p>95% of observations are in range between {{ sigmas[1][0] }} and {{ sigmas[1][1] }}</p> #}
        {# <p>99% of observations are in range between {{ sigmas[2][0] }} and {{ sigmas[2][1] }}</p> #}
        {# <a href="{{ path('home') }}"> #}
        {# <button>Close report</button> #}
        {# </a> #}
        {# </div> #}
        {# <div class="column-7 relative"> #}
        {# <div class="scrollable"> #}
        {# <p>text</p> #}
        {# </div> #}
        </div>
    {% endfor %}
{% endblock %}